# Copiar a `.env` solo en tu máquina (no se sube a git).
# En build/release usar --dart-define o --dart-define-from-file=.env
# Valores: Supabase Dashboard → Settings → API → Project URL y anon public key.
#
# NUNCA usar service_role en la app Flutter.

SUPABASE_URL=
SUPABASE_ANON_KEY=
